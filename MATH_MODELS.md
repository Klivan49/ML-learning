# Математические модели машинного обучения

В проекте File Sorter используются три модели классификации из библиотеки
scikit-learn: логистическая регрессия, случайный лес и градиентный бустинг.
Ниже приводится подробное математическое описание каждой модели.

---

## 1. Логистическая регрессия (Logistic Regression)

### 1.1 Постановка задачи

Пусть дана обучающая выборка `(x_i, y_i)`, `i = 1..N`, где
`x_i ∈ ℝⁿ` — вектор признаков, `y_i ∈ {1, …, K}` — метка класса
(K = 6 для профиля general, K = 10 для education).

Логистическая регрессия моделирует **апостериорные вероятности** классов:

```
P(y = k | x)  — вероятность того, что объект x принадлежит классу k
```

### 1.2 Softmax-преобразование (Multinomial Logit)

Для задачи многоклассовой классификации используется **softmax-функция**:

```
z_k = w_kᵀ·x + b_k                 — линейная комбинация (логит) для класса k

P(y = k | x) = exp(z_k) / Σⱼ exp(z_j)     — softmax
```

Здесь `w_k ∈ ℝⁿ` — вектор весов для класса k, `b_k ∈ ℝ` — смещение.
В сумме `Σⱼ` пробегает все классы от 1 до K.

Свойства softmax:
- `0 ≤ P(y=k|x) ≤ 1` для всех k
- `Σₖ P(y=k|x) = 1`
- Функция **дифференцируема** по всем `w_k` и `b_k`

### 1.3 Функция потерь: Cross-Entropy (Log Loss)

Для одного обучающего примера `(x, y)` с истинным классом `y = t`:

```
L(w, b) = -log(P(y = t | x))
        = -z_t + log(Σⱼ exp(z_j))
```

Полная функция потерь на всей выборке (среднее):

```
J(w, b) = -(1/N) · Σᵢ [ z_{t_i}^{(i)} - log(Σⱼ exp(z_j^{(i)})) ]
```

где `z_k^{(i)} = w_kᵀ·x_i + b_k`.

### 1.4 Связь с MLE

Минимизация кросс-энтропии эквивалентна **максимизации правдоподобия (MLE)**:

```
L(w, b) = Πᵢ P(y = y_i | x_i)     — правдоподобие

log L(w, b) = Σᵢ log P(y = y_i | x_i) = -N·J(w, b)

argmax log L(w, b) = argmin J(w, b)
```

### 1.5 Градиентный спуск

Градиент J по весам класса k для одного примера:

```
∂J / ∂w_k = (P(y=k|x) - δ_{k,t}) · x

где δ_{k,t} = 1, если k = t, и 0 иначе.
```

Обновление весов (SGD / mini-batch):

```
w_k ← w_k - α · (1/m) · Σᵢ (P(y=k|x_i) - δ_{k,t_i}) · x_i
```

где `α` — learning rate, `m` — размер батча.

### 1.6 Многоклассовый случай: One-vs-Rest (OvR) vs Multinomial

В проекте используется режим **multinomial** (softmax), при котором
обучается единая матрица весов `W ∈ ℝ^{K × n}`, а не K бинарных
классификаторов (как в OvR).

### 1.7 Регуляризация (L2)

Для борьбы с переобучением добавлен L2-регуляризатор:

```
J_reg(w, b) = J(w, b) + (1/2C) · Σₖ ||w_k||₂²

где C — обратный коэффициент регуляризации (C = 1.0 в конфиге).
```

Чем меньше C, тем сильнее регуляризация (штраф за большие веса).

### 1.8 Стандартизация признаков (StandardScaler)

Логистическая регрессия чувствительна к масштабу признаков, поэтому
перед ней применяется StandardScaler:

```
z = (x - μ) / σ

где μ — среднее, σ — стандартное отклонение на обучающей выборке.
```

Это гарантирует, что градиентный спуск сходится быстрее, а веса
интерпретируемы (их величина отражает важность признака).

### 1.9 Граница решения

Решение принимается по максимуму апостериорной вероятности:

```
ŷ = argmax_k P(y = k | x)
```

Граница между классами k и j — гиперплоскость в ℝⁿ:

```
(w_k - w_j)ᵀ·x + (b_k - b_j) = 0
```

---

## 2. Случайный лес (Random Forest)

### 2.1 Базовый элемент: решающее дерево

Каждое дерево в случайном лесу — это бинарное дерево решений,
где каждый внутренний узел содержит правило:

```
if x_[feature] <= threshold:
    → левый потомок
else:
    → правый потомок
```

Листья дерева содержат распределение классов:

```
p_k = (число объектов класса k в листе) / (всего объектов в листе)
```

### 2.2 Критерий качества разбиения: Gini impurity

Выбор признака и порога в каждом узле минимизирует **Gini impurity**:

```
Gini(S) = 1 - Σₖ p_k²

где p_k — доля объектов класса k в множестве S.
```

Для разбиения S → S_left ∪ S_right:

```
Gini_split(S) = (|S_left|/|S|) · Gini(S_left) + (|S_right|/|S|) · Gini(S_right)
```

Выбирается разбиение, минимизирующее Gini_split.

Альтернатива — **энтропия (information gain)**:

```
H(S) = -Σₖ p_k · log(p_k)

IG(S) = H(S) - Gini_split(S)
```

В проекте используется критерий **Gini** (параметр `criterion='gini'`).

### 2.3 Ансамбль: Bagging (Bootstrap Aggregating)

Случайный лес состоит из T деревьев (в проекте T = 100).

**Bagging:** каждое дерево обучается на своей случайной подвыборке:

1. Из исходной выборки N объектов формируется bootstrap-выборка:
   - N раз выбирается случайный объект с возвращением
   - В среднем, ~63.2% уникальных объектов попадают в bootstrap
   - Остальные ~36.8% — Out-of-Bag (OOB) для оценки

2. **Random subspace:** при построении каждого узла рассматривается
   не все n признаков, а случайное подмножество `mtry = √n`
   (для классификации) или `mtry = n/3` (для регрессии). В проекте
   `max_features='sqrt'`.

### 2.4 Агрегация предсказаний

Для классификации — **голосование (majority voting)**:

```
ŷ = argmax_k (1/T) · Σₜ I(предсказание t-го дерева = k)
```

где `I(·)` — индикаторная функция.

### 2.5 OOB-оценка

Каждое дерево t оценивается на объектах, не попавших в его
bootstrap-выборку (OOB-объекты). OOB-ошибка:

```
OOB_error = (1/N) · Σᵢ I(ŷ_i^{OOB} ≠ y_i)

где ŷ_i^{OOB} — предсказание по деревьям, для которых i был OOB.
```

OOB-оценка — несмещённая оценка обобщающей способности без
необходимости в отдельной валидационной выборке.

### 2.6 Важность признаков (Feature Importance)

Важность признака j вычисляется как **суммарное снижение Gini**
во всех узлах, где использовался признак j, усреднённое по всем деревьям:

```
Importance(j) = (1/T) · Σₜ Σ_{узел ∈ t, feature = j} ΔGini(узел)

ΔGini(узел) = Gini_до - Gini_после
```

### 2.7 Bias-Variance Tradeoff

- **Bias (смещение):** каждое отдельное дерево имеет высокую дисперсию
  и низкое смещение (глубокая настройка на данные)
- **Variance (дисперсия):** bagging снижает дисперсию в T раз
  (за счёт усреднения некоррелированных деревьев)
- Случайный подбор признаков дополнительно декоррелирует деревья,
  что ещё сильнее снижает дисперсию

Итог: случайный лес устойчив к переобучению и работает «из коробки»
без тонкой настройки гиперпараметров.

---

## 3. Градиентный бустинг (Gradient Boosting)

### 3.1 Идея: аддитивная модель в функциональном пространстве

Градиентный бустинг строит ансамбль последовательно:

```
F_M(x) = Σ_{m=0}^{M} γ_m · h_m(x)

где:
- h_0(x) = const — начальное приближение (константа)
- h_m(x) — слабый ученик (обычно дерево малой глубины)
- γ_m — шаг (learning rate)
- M — число итераций (n_estimators)
```

### 3.2 Оптимизация градиентным спуском в функциональном пространстве

На каждом шаге m мы имеем текущую модель F_{m-1}(x).
Определим **псевдо-остатки** (pseudo-residuals) как антиградиент
функции потерь L(y, F) в точке F = F_{m-1}(x):

```
r_{im} = -∂L(y_i, F(x_i)) / ∂F(x_i) |_{F = F_{m-1}}
```

Для многоклассовой классификации с cross-entropy:

Функция потерь для одного примера (K классов):

```
L(y, F) = -Σ_{k=1}^{K} y_k · log(p_k)

где p_k = exp(F_k) / Σⱼ exp(F_j) — softmax
      y_k ∈ {0, 1} — one-hot кодировка истинного класса
      F_k — значение модели для класса k
```

Псевдо-остатки для класса k объекта i:

```
r_{ikm} = y_{ik} - p_{ik}^{(m-1)}

где p_{ik}^{(m-1)} = softmax(F_{m-1}^{(k)}(x_i))
```

То есть псевдо-остатки — это разница между истинным one-hot
вектором и текущим предсказанием softmax. Интуитивно: мы учимся
исправлять ошибки вероятностной модели.

### 3.3 Алгоритм

```
Вход: обучающая выборка {(x_i, y_i)}_{i=1}^{N}
Параметры: M (число итераций), ν (learning rate), J (глубина дерева)

1. Инициализация:
   F_0^{(k)}(x) = log(π_k) — логарифм априорной частоты класса k

2. Для m = 1..M:
   a. Для каждого класса k = 1..K:
      - Вычислить псевдо-остатки:
        r_{ikm}^{(k)} = y_{ik} - p_{ik}^{(m-1)}
      - Обучить дерево h_m^{(k)}(x) глубины J на псевдо-остатках:
        h_m^{(k)} = argmin Σ_i (r_{ikm}^{(k)} - h(x_i))²
      - Определить оптимальный шаг γ_m:
        γ_m = argmin_γ Σ_i L(y_i, F_{m-1}^{(k)}(x_i) + γ · h_m^{(k)}(x_i))
      - Обновить модель:
        F_m^{(k)}(x) = F_{m-1}^{(k)}(x) + ν · γ_m · h_m^{(k)}(x)
   (ν = learning_rate — коэффициент shrinkage)

3. Выход: F_M(x) = (F_M^{(1)}(x), ..., F_M^{(K)}(x))
```

**Важно:** в популярной реализации (scikit-learn) деревья строятся
по всем K классам сразу — каждое дерево имеет K листьевых значений,
одно для каждого класса. Псевдо-остатки считаются независимо для
каждого класса.

### 3.4 Ограничение глубины деревьев

В отличие от случайного леса, деревья в бустинге — «слабые»,
обычно глубины 3-5 (в проекте max_depth=3). Это контролирует
сложность модели и предотвращает переобучение.

### 3.5 Shrinkage (Learning Rate)

Коэффициент ν ∈ (0, 1] регулирует вклад каждого дерева:

```
F_m = F_{m-1} + ν · γ_m · h_m
```

Меньший ν → больше деревьев требуется для сходимости, но
обобщающая способность выше. В проекте ν = 0.1, M = 100.

Компромисс: число деревьев M и learning rate ν связаны —
рекомендуется M ≈ 100-1000 при ν = 0.01-0.1.

### 3.6 Subsampling (Stochastic Gradient Boosting)

Для дополнительной регуляризации используется subsample (доля
обучающей выборки, используемая на каждой итерации). В проекте
`subsample=1.0` (все данные), но можно уменьшить для борьбы с
переобучением.

### 3.7 Loss function: Deviance

Для классификации используется логарифмическая функция потерь
(deviance / cross-entropy):

```
Deviance = -2 · Σᵢ Σₖ y_{ik} · log(p_{ik})
```

Она согласована с вероятностной природой softmax и является
стандартной для многоклассовой классификации в градиентном
бустинге.

### 3.8 Bias-Variance Tradeoff в бустинге

- Бустинг последовательно снижает **bias**: каждое новое дерево
  «дожимает» ошибки предыдущих
- При этом **variance** растёт с числом итераций — риск переобучения
- Регуляризация: малая глубина деревьев + learning rate + subsampling

### 3.9 Сравнение со случайным лесом

| Характеристика | Случайный лес | Градиентный бустинг |
|---|---|---|
| Bias | Средний | Низкий (снижается итеративно) |
| Variance | Низкий (усреднение) | Высокий (нужна регуляризация) |
| Параллелизация | Полная (деревья независимы) | Последовательная (сложно параллелить) |
| Гиперпараметры | Мало, устойчив «из коробки» | Много, требует настройки |
| Переобучение | Редко | Часто (без регуляризации) |
| Интерпретация | Важность признаков | Важность признаков |
| Точность (в среднем) | Высокая | Часто выше при правильной настройке |

---

## 4. Метрики оценки

### 4.1 Accuracy (доля правильных ответов)

```
Accuracy = (1/N) · Σᵢ I(ŷ_i = y_i)
```

Недостаток: неинформативна при несбалансированных классах.

### 4.2 Precision (точность) для класса k

```
Precision_k = TP_k / (TP_k + FP_k)
```

Доля объектов, названных классом k, которые действительно
относятся к классу k.

### 4.3 Recall (полнота) для класса k

```
Recall_k = TP_k / (TP_k + FN_k)
```

Доля объектов класса k, которые были правильно обнаружены.

### 4.4 F1-score (макро-усреднение)

F1 для класса k — гармоническое среднее precision и recall:

```
F1_k = 2 · (Precision_k · Recall_k) / (Precision_k + Recall_k)
```

**Macro F1** — среднее арифметическое F1 по всем классам:

```
F1_macro = (1/K) · Σₖ F1_k
```

Каждый класс учитывается с равным весом, независимо от числа
примеров. Это целевая метрика проекта.

### 4.5 Матрица ошибок (Confusion Matrix)

```
C_{ij} = число объектов класса i, предсказанных как класс j
```

Позволяет визуально оценить, какие классы модель путает между собой.

---

## 5. Почему 48 признаков?

Итоговый вектор признаков `x ∈ ℝ⁴⁸` формируется как конкатенация:

| Группа | Размерность | Описание |
|---|---|---|
| Filename features | 1 + 1 + 1 + 1 + 1 + 6 = 11 | Длина, токены, цифры, слова, дата, 6 keyword flags |
| Size features | 4 + 5 = 9 | bytes, log, KB, MB + 5 one-hot size classes |
| Extension features | 7 + 6 = 13 | 7 ext groups + 6 ext categories (one-hot) |
| Text content features | 5 | Длина, слова, строки, unique ratio, avg word len |
| Binary content features | 4 | Энтропия, хеш, zero ratio, printable ratio |
| **Итого** | **11 + 9 + 13 + 5 + 4 = 42** | — |

Фактически 42 базовых + 6 keyword flags = 48 признаков.
Keyword-флаги бинарные (есть ли ключевое слово в имени).

Feature engineering играет ключевую роль: признаки спроектированы
так, чтобы отражать различные аспекты файла (имя, размер,
расширение, содержимое) без необходимости в глубоких нейросетях.

---

## Литература

1. Hastie, Tibshirani, Friedman. *The Elements of Statistical Learning* (2nd ed.)
2. Bishop. *Pattern Recognition and Machine Learning*
3. Breiman. *Random Forests* — Machine Learning, 45(1):5-32, 2001
4. Friedman. *Greedy Function Approximation: A Gradient Boosting Machine* — Annals of Statistics, 2001
5. scikit-learn documentation: https://scikit-learn.org/stable/modules/ensemble.html
